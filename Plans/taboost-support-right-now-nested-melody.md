# Plan — Genie 3 bugs: the code is already fixed; bust the cache so it loads

## Context

The three Genie bugs in this prompt were already fixed and pushed to `genie-preview`
(commit `7cf21c94`, last session). Re-investigation confirms:

- **All three fixes are present at HEAD and pushed** (local HEAD == `origin/genie-preview`):
  - Bug 1 — `css/genie.css:170` `@media (hover: hover)` guard on `.genie-chip:hover`
  - Bug 2 — `js/genie.js:340` `applyNiche()` scrolls to `#search-results-container`
  - Bug 3 — `index.html:1378` `window.showPriorityCampaigns()` + `js/genie.js:376` Genie chip calls it
- There is **no separate `taboost-genie-preview` repo and no `demo` branch** — only
  `origin = ceyre-boop/TABOOST-Shop`, branch `genie-preview` (placeholders in the prompt).

So why do the bugs still appear? **Stale cache.** `index.html` references the assets with bare
URLs — `css/genie.css`, `js/scoring.js`, `js/genie.js` — **no `?v=` cache-bust anywhere** in the
file. CLAUDE.md's own non-negotiable: *"Always cache-bust before pushing… without it, creators see
stale CDN-cached numbers."* The fixed `genie.js`/`genie.css` are on the server, but the browser /
Render CDN keep serving the old cached copies. **That is the actual blocker, not the code.**

Intended outcome: force the corrected `genie.js`/`genie.css` to load so the three already-shipped
fixes take effect for everyone (you + Marco) without manual hard-refresh.

> Branch: stays on `genie-preview`. `main` is not touched.

---

## Change — add cache-busting to the code assets (`index.html`, 3 lines)

Append a version query to the three code assets so a new deploy invalidates the cache:

| Line | From | To |
|------|------|----|
| `index.html:27` | `href="css/genie.css"` | `href="css/genie.css?v=20260625"` |
| `index.html:1044` | `src="js/scoring.js"` | `src="js/scoring.js?v=20260625"` |
| `index.html:1554` | `src="js/genie.js"` | `src="js/genie.js?v=20260625"` |

Notes:
- `product-data.js` is left as-is — it's regenerated by the data pipeline and `genie.js`'s
  `showFreshness()` fetches it by bare path; not part of these bugs.
- The three bug fixes themselves need **no code change** — they're already correct and in place.
  (I did re-confirm the class-strip the prompt asks for in Bug 1 would be a no-op: chips are
  recreated every render and nothing ever adds `.active`/`.selected`; the red was sticky `:hover`,
  already handled by the `@media (hover: hover)` guard.)
- Commit will stage **only `index.html`** (and this plan). The unrelated working-tree changes
  (`settings.json`, `CLAUDE.md`, deleted `images/*.jpg`) are left untouched.

Future-proofing (noted, not done now): these `?v=` stamps are manual — any later genie.js/css edit
needs the stamp bumped, or wire it into a deploy step. Out of scope for this fix.

---

## Verification

1. **Confirm assets present** locally: `node --check js/genie.js` (syntax), and the three greps
   that already passed (`hover: hover` in genie.css, `search-results-container` in genie.js,
   `showPriorityCampaigns` in index.html + genie.js).
2. After push + Render rebuild, load the preview and check the genie.js request URL carries
   `?v=20260625` (Network tab) — proves the fresh file is served, not the cache.
3. Behavior (mobile / touch device or emulation — sticky hover won't repro with a mouse):
   - Genie → Find a product → **Fashion** → "Jewelry Accessories" shows **no red** on load;
     ← Categories → **Toys & Baby** → no red; bounce between categories → no carry-over.
   - Tap any subcategory → **panel closes + scrolls to the product results**.
   - Run a search, then Genie → **Priority Campaigns** → campaign tiles come back into view.
4. If the bugs *still* show after the `?v=` URL is confirmed loading fresh, then a code diagnosis
   was wrong (most likely Bug 1) — re-open with a real browser repro. Interceptor would let me do
   this myself; its install is still pending your authorization to clone the source repo.

Ship:
```bash
git add index.html
git commit -m "chore: cache-bust genie.css/js + scoring.js so shipped Genie fixes load (stale CDN)"
git push origin genie-preview
```

## Out of scope
- Re-implementing the three fixes (already done in `7cf21c94`).
- Automated cache-bust tooling for index.html assets.
- Scoring rating-fidelity tuning; mobile header work (already committed by you).
